# express_proxy
Proxy Express server for uploading files

- `npm install`
- Create `.env` file

```
PORT=3000
NODE_ENV=development
KEY=test
```
- Create `files` directory in project root
- `npm run server`
